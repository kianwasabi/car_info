# Config file for the CarInfo package
# Exports the following targets:
#   CarInfo - CMake target for CarInfoService
# Additionally, the following variables are defined:
#   CarInfo_VERSION - The CarInfo version number

# Compute paths
get_filename_component(CARINFO_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)

# Our library dependencies (contains definitions for IMPORTED targets)
# include("${CARINFO_CMAKE_DIR}/CarInfoTargets.cmake") # Uncomment if needed

# Legacy variable, kept for compatibility
# get_target_property(CARINFO_INCLUDE_DIRS CarInfo INTERFACE_INCLUDE_DIRECTORIES)

set(CARINFO_VERSION @PACKAGE_VERSION@)
set(CARINFO_VERSION_STRING "@PACKAGE_VERSION@")

